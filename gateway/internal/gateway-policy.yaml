apiVersion: networking.gke.io/v1
kind: GCPGatewayPolicy
metadata:
  name: nb-clone-internal-gateway-policy
  namespace: default
spec:
  default:
    sslPolicy: syst-nb-clone-regional-ssl-policy
  targetRef:
    group: gateway.networking.k8s.io
    kind: Gateway
    name: nb-clone-internal-gateway
