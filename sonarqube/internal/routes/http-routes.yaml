apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: nb-clone-sonarqube-internal
  namespace: ns-nb-clone-sonarqube
spec:
  parentRefs:
  - name: nb-clone-internal-gateway
    namespace: default
  hostnames:
  - "sonarqube-clone.brm.gcp.internal.skycdp.de"
  rules:
  - matches:
    - path:
        type: PathPrefix
        value: /
    backendRefs:
    - name: nb-clone-sonarqube-internal
      port: 9000