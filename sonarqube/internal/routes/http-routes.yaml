apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: nb-clone-sonarqube-internal
  namespace: ns-nb-clone-sonarqube
spec:
  parentRefs:
  - name: nb-clone-internal-gateway
    namespace: default
  hostnames:
  - "sonarqube-clone.dev.internal.example.de"
  rules:
  - matches:
    - path:
        type: PathPrefix
        value: /
    backendRefs:
    - name: nb-clone-sonarqube-internal
      port: 9000
